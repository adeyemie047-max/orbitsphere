# Neon PostgreSQL (free tier)
# Pooled connection — use for app runtime (serverless / Next.js)
DATABASE_URL="postgresql://USER:PASSWORD@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"

# Direct connection — use for Prisma migrations only
DIRECT_URL="postgresql://USER:PASSWORD@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# Auth.js v5
AUTH_SECRET="generate-with-openssl-rand-base64-32"
AUTH_URL="http://localhost:3000"
# Set to true on Vercel (also set via auth.config trustHost)
AUTH_TRUST_HOST="true"

# Public site URL (sitemap, JSON-LD, Open Graph)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Google OAuth (optional)
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# Cloudinary — image uploads for articles (optional in dev)
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# Cron — scheduled article publishing (Vercel Cron / external scheduler)
CRON_SECRET="generate-with-openssl-rand-base64-32"

# Web Push (VAPID) — generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:newsroom@orbitsphere.ng"
